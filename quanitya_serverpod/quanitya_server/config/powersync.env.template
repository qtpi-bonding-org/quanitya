# PowerSync JWT Configuration for Community Server
# Generate keys with: dart run tool/generate_keys.dart

# JWT signing key (base64 encoded JWK with private key)
# If not set, uses default development key
SERVERPOD_JWT_SIGNING_KEY=

# PowerSync service configuration
POWERSYNC_URL=http://localhost:8105

# Environment
ENVIRONMENT=development